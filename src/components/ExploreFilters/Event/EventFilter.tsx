import './EventFilter.scss';

const EventFilter = () => {
    return (
        <div className="c-eventfilter">
            This should only show if event filter has been toggled.
        </div>
    )
}

export default EventFilter;