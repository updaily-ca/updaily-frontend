import './BusinessFilter.scss';

const BusinessFilter = () => {
    return (
        <div className="c-businessfilter">
            This should only show if business filter has been toggled.
        </div>
    )
}

export default BusinessFilter;